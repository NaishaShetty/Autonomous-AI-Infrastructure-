"""ACTIVE Phase 4.2 -- failure pattern learning on the real-data foundation.

New package, deliberately distinct from both ``src/patterns/`` (old,
frozen, historical Phase 4.2 -- never imported here) and
``src/failure_experience/`` (active Phase 4.1 -- a dependency of this
package, not a peer). See ``configs/phase4_2_active_pattern_protocol.json``
for the frozen experimental protocol and
``docs/PHASE4_2_ACTIVE_FAILURE_PATTERNS.md`` for the full research report.
"""

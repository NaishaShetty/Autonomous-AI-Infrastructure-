# Weakness Register (running)

Updated after Step 1 (Engineering Correctness) and Step 2 (Observability).
Status values: FIXED / IMPROVED / INVESTIGATED — NOT SOLVED / ACCEPTED
LIMITATION / BLOCKED BY EXTERNAL INFRASTRUCTURE / NOT YET STARTED.

| ID | Weakness | Status | Evidence |
|---|---|---|---|
| P3-W8 | RNG/test-order defect in prediction eval | **FIXED** | `hash()`→`hashlib` seed fix; regression test across 3 `PYTHONHASHSEED` values (`reports/ENGINEERING_FIXES_REPORT.md` #1) |
| P3-W7 | GPU probe nondeterminism/classification | **FIXED** | `src/phase4/gpu_probe.py` explicit 5-state classification + provenance; test-only forced override (`reports/ENGINEERING_FIXES_REPORT.md` #2) |
| P1-W4 | Full-suite instability | **IMPROVED** | 806/5-failed → 815/0-failed across 3 confirmation runs; 1 original failure logged as investigated-not-reproduced, not suppressed (`reports/ENGINEERING_FIXES_REPORT.md` #5) |
| P5-W2 (persistence engineering precondition) | Memory store durability/isolation | **FIXED (pre-existing, verified)** | Reviewed `src/phase4/memory.py`: real SQLite, schema versioning, scope/temporal/cross-run isolation already correct; no code change needed. Actual repeated-incident ON/OFF experiment is Step 6, not yet started |
| P3-W1 | Predictive-observability audit | **FIXED** (audit delivered) | `audits/P3_PREDICTIVE_OBSERVABILITY_AUDIT.md` — per-class precursor/window/gap classification for all 8 failure classes |
| P3-W2 | Expand legitimate decision-time telemetry | **IMPROVED** | Fixed broken cross-platform RSS (was silently constant-zero on Windows); added `process_cpu_percent`/`process_age_seconds`/`system_available_memory_bytes`/`system_memory_percent`; added real pre-flight resource-availability probe for `resource_unavailable` (previously zero telemetry window). Raw telemetry only — not yet wired into evaluated features (that's Step 3) |
| P3-W3 | Determine whether failure classes are actually predictable | **INVESTIGATED** | `reports/P3_PREDICTABILITY_REMEDIATION_REPORT.md` — `cpu`/pooled `oom`/`flaky`: NOT VALIDATED; `resource_unavailable`: STRONG EVIDENCE; `oom` ≥2-sample subset: PLAUSIBLE/UNCONFIRMED |
| P3-W4 | Simple-model-first methodology | **FIXED** | `StandardScaler`+`LogisticRegression` only, per protocol; no complex model introduced (no evidence justified one) |
| P3-W5 | Prevent "always fires" false predictor | **FIXED** | False-alarm-rate/specificity computed and used to override a naive AUROC-only verdict for `cpu`/`oom`/`flaky` (all `false_alarm_rate == 1.0`, correctly disqualified) |
| P3-W6 | Rolling-checkpoint / lead-time evaluation | **IMPROVED (pre-existing mechanism verified + used)** | `compute_metrics` already scores at every rolling checkpoint per run and reports lead time / detection-before-failure rate; used as-is this step, not newly built |
| P3-W1 (oom sub-finding) | oom's own docstring hypothesis (0–1 samples before outcome) | **CONFIRMED empirically** | Observability-split re-evaluation: ≥2-sample subset separates from its shuffled control; 0–1-sample subset does not |
| P4-W1 | Environment generalization currently fails (do not solve by training on held-out) | **INVESTIGATED / IMPROVED for OOM** | Held-out/robustness never fit on (verified in script); pre-remediation 0.678→0.506 OOM numbers do not replicate under corrected telemetry (honest dev AUROC only 0.434 pre-fix) — see `reports/P4_GENERALIZATION_REMEDIATION_REPORT.md` |
| P4-W2 | Build environment-aware representations (A/B/C) | **FIXED for OOM, root cause identified** | `rss_ratio_env_normalized` (resource-limit-aware feature, `src/phase4/prediction_features_env.py`) raises OOM AUROC 0.434→0.989 (dev), with only 0.006/0.054 degradation to held-out/robustness. `cpu`/`resource_unavailable`/`flaky`: no environment-normalization problem identified, confirmed no material A vs B difference |
| P4-W3 | Distribution-shift robustness testing | **IMPROVED (partial)** | 3 pre-existing genuinely-distinct environments (differing resource limits, telemetry resolution, dependency contention, deadlines) reused; degradation measured for both models. Threshold-recalibration-per-environment not yet explored (flagged as follow-up) |
| P4-W4 | Determine whether environment conditioning solves it | **CONFIRMED for OOM specifically** | Richer/environment-aware telemetry (known resource limits) resolves the OOM generalization collapse; not yet tested for other families |
| P4-W5 | Controlled environment scope (Docker/etc.) | NOT ATTEMPTED | Still local controlled runtimes, not containerized; honestly out of scope for this remediation phase per the master register's own boundary (do not conflate with a real cluster) |
| P1-W1 | Sentiment uncertainty weaker than arithmetic/QA | **INVESTIGATED — NOT SOLVED (root cause identified)** | `reports/P1_P2_AGENT_REMEDIATION_REPORT.md` — all 4 candidates rank-equivalent (AUROC identical, mathematically expected for binary classification); temperature scaling fixed calibration (ECE 0.089→0.023) but not discrimination. Accepted limitation, not locally fixable by estimator choice |
| P1-W2 | Real-model coverage | **ACCEPTED, no action needed** | 3 local models unchanged; expansion not justified by this step's findings |
| P1-W3 | Mechanism-aware uncertainty interface | **FIXED (pre-existing, verified)** | `uncertainty_eval.py`'s per-mechanism interface confirmed working across 4 sentiment candidates |
| P2-W1 | Expand held-out sample size for calibrated policy | NOT YET STARTED | Follow-up |
| P2-W2 | RETRY only where legitimate per task | **FIXED (pre-existing, verified)** | Confirmed RETRY defined only for arithmetic agent; no fabricated RETRY for sentiment/QA |
| P2-W3 | Retry economics sensitivity sweep | NOT YET STARTED | Follow-up |
| P5-W1 | Memory ON/OFF repeated-incident experiment | **FIXED — hypothesis confirmed** | `reports/MEMORY_REMEDIATION_REPORT.md` — memory ON switches action at exactly episode 2 (earliest structurally possible) and self-corrects permanently; memory OFF never switches, never recovers, across 6 episodes each |
| P5-W2 | Memory persistence/restart/isolation | **FIXED (pre-existing, verified under real restart)** | Version preserved (1==1), record retrievable, cross-workload query correctly empty, after full Python-object teardown and file reopen |
| P5-W3 (3 full-suite failures) | Re-evaluate each independently | See `reports/ENGINEERING_FIXES_REPORT.md` #5 for the one that recurred; others resolved by Step 1/2 fixes | Full-suite confirmation run in progress |
| (new, found in Step 7) | `cpu`-family real subprocess-timing margin defect | **FIXED** (margin fix + auto-widening backstop) | `reports/ADDENDUM_CPU_TIMING_DEFECT.md` — "fast" duration choices left only ~70-100ms margin against measured ~65-75ms subprocess-startup overhead, causing systematic (not random) false timeouts, deterministically reproduced. Margin widened; since real overhead still varies run to run (29% observed on one later run), `train_and_persist`/`train_and_persist_scope_router` also gained a bounded, deterministic auto-widen-and-retry fallback instead of hard-failing. Also affects Step 3/4's `cpu` numbers, flagged as requiring re-confirmation (not yet re-run in this phase) |
| (new, found in Step 7) | Real nondeterminism in `run_agent_task`'s prediction-boundary temporal cut | **FIXED — high severity** | `reports/ADDENDUM_TIMESTAMP_TIE_DETERMINISM_DEFECT.md` — strict `<` against microsecond timestamps could silently drop the last (most-informative) self-consistency sample when it tied the failure event's own timestamp, non-deterministically changing ANSWER-vs-REVIEW/ABSTAIN decisions for the highest-risk episodes. Fixed to `<=` (matching the codebase's existing convention elsewhere); verified deterministic across repeated runs; regression test added |
| P1-W4 / P5-W3 (full-suite instability, final confirmation) | Full-suite stability after all Step 7 fixes | **FIXED — confirmed clean** | 4 consecutive full-suite runs during Step 7 investigation each surfaced a genuine, distinct, now-fixed defect (cpu timing margin, agent-task timestamp-tie nondeterminism, single-class corpus edge case); final isolated run: **837 passed, 0 failed** (53 min) |
| (new, found in Step 7) | Test assertion didn't account for legitimate ABSTAIN-skips-diagnosis path in `run_workload` | **FIXED** | `reports/ADDENDUM_ABSTAIN_SKIPS_DIAGNOSIS_TEST_GAP.md` — not an engineering defect, the pipeline behaved as designed; test relaxed to only require diagnosis follow-through when the decision wasn't ABSTAIN. Verified 3/3 reruns |
| P5-W4 | Recovery/action taxonomy expansion | NOT YET STARTED | Existing actions (retry/reconfigure/rollback/escalate/abstain) exercised; no new mechanism evaluated this phase |
| Full re-evaluation, final audits | — | **COMPLETE** | Step 7 — see `audits/FINAL_SYSTEM_CHECK.md` (15-item checklist) and `FINAL_POST_REMEDIATION_EVALUATION.md`. Test-order independence confirmed: 837/837 passed under reversed directory order (1h03m53s) |

This file will be superseded by a complete `WEAKNESS_REGISTER.md` at the
root of this run directory once all 7 steps are complete; it is kept
up to date incrementally so progress is never only implicit in chat.
